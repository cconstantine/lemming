#!/usr/bin/env ruby

require 'rubygems'
#$LOAD_PATH.unshift File.join(File.dirname(__FILE__), '..', 'lib')
require 'lemming'

lemming = Lemming::Lemming.new :command => ARGV
lemming.run
lemming.results.report

exit(0)